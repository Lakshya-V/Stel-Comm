# MediaPipe Hand Landmarker

This project uses the MediaPipe Hand Landmarker task with OpenCV to detect hand landmarks from a webcam.

## Model File

The program requires the `hand_landmarker.task` model file in the project root, beside `detect.py`.

The model is downloaded separately from the official [MediaPipe Hand Landmarker documentation](https://developers.google.com/edge/mediapipe/solutions/vision/hand_landmarker).

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python detect.py
```

Press `q` to stop the webcam window.

tactical_gesture_mesh/
│
├── models/                         # Model binaries & trained weights
│   ├── hand_landmarker.task        # Google MediaPipe Hand Landmarker model
│   └── tactical_mlp.pth            # Trained PyTorch model weights (Generated by train.py)
│
├── data/                           # Datasets & Analysis outputs
│   ├── raw_csv/
│   │   └── tactical_landmarks.csv  # Raw recorded landmark dataset (Generated by collect_data.py)
│   └── processed/
│       └── pca_clusters.png        # EDA cluster visualization (Generated by eda.py)
│
├── src/                            # Core Machine Learning & Utilities Module
│   ├── __init__.py
│   ├── collect_data.py             # Phase 1: Webcam data recorder
│   ├── eda.py                      # Phase 1: Dataset analysis & cluster visualizer
│   ├── dataset.py                  # Phase 1: PyTorch Dataset & DataLoader splitter
│   ├── model.py                    # Phase 1: Lightweight MLP neural network class
│   ├── train.py                    # Phase 1: Training script & .pth weight exporter
│   ├── gesture_detector.py         # Phase 2: Live MediaPipe + MLP inference wrapper
│   ├── network_mesh.py             # Phase 2: NetworkX graph simulation & routing logic
│   └── socket_comm.py              # Phase 2: Socket client/server for inter-laptop messaging
│
├── sender_app.py                   # Phase 2 Main App: Laptop A (Tactical Camera & Sender)
├── receiver_app.py                 # Phase 2 Main App: Laptop B (Mesh Visualizer & Receiver)
│
├── requirements.txt                # Python dependencies
└── README.md                       # Project instructions
